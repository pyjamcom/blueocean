{
  "name": "escapers",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace apps/web",
    "dev:server": "npm run dev --workspace apps/server",
    "dev:all": "npm run dev:web & npm run dev:server",
    "build:web": "npm run build --workspace apps/web",
    "build:server": "npm run build --workspace apps/server",
    "lint": "eslint .",
    "typecheck": "npm run typecheck --workspace apps/web && npm run typecheck --workspace apps/server",
    "test": "node tests/validate_questions.spec.js && node tests/ws_payloads.spec.js && node tests/validate_assets.spec.js && node tests/no_text_config.spec.js",
    "load:ws": "node scripts/load_simulator.js"
  },
  "devDependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "ws": "^8.16.0"
  },
  "engines": {
    "node": ">=18"
  }
}
